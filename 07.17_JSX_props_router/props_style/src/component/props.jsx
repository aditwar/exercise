export default function Props({ name }) { // { name } ini merupakan obj (props) yg dipanggil dari App.jsx
    return(
        <>
            <h1>Componen Props { name }</h1>{/* ini untuk memunculkan Prop yg sudah diimport */}
        </>
    )
}